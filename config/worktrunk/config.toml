worktree-path = "{{ repo_path }}/../{{ repo }}.{{ branch | sanitize }}/{{ repo }}"

[pre-start]
init = '''exec "$XDG_CONFIG_HOME/worktrunk/wt-pre-start"'''

[aliases]
new = "wt switch --create --no-cd {{ args }}"
review = "wt switch --no-cd {{ args }}"
